/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['example.com', 'random.imagecdn.app', 'picsum.photos'], // Fügen Sie hier alle Domains hinzu, von denen Sie Bilder laden.
      },
  }

// next.config.mjs

export default nextConfig;